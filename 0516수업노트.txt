리액트의 jsx컴포넌트는 함수라서 호출해야 실행(렌더)됨
컴포넌트는 함수로 이루어져있고
부모와 자식관계를 갖고있다
내부적으로 사용하는 변수와 함수들도 있다.

함수내부의 값이 변경시 해당 함수를 호출하는 것을 알아야함

useState는 단순히 상태값을 바꾸는것 뿐 아니라
상태값 변경시 컴포넌트를 리렌더함->퍼포먼스 영향을 미침

함수의 호출
원시타입
비원시타입
